{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TuiOnlyEvent",
  "description": "TUI-exclusive events.\n\nThese events are dropped by SDK and App-Server consumers. They drive overlays, toasts, and UI-only state transitions that are not part of the protocol contract.\n\nPer `event-system-design.md` Section 1.7, the design listed this type as owned by `coco-tui`. Since `CoreEvent::Tui(TuiOnlyEvent)` is part of the envelope enum defined in `coco-types`, the type itself must live in `coco-types` to avoid a cyclic dependency. The semantic contract (TUI-only, never sent to SDK) is preserved via consumer dispatch rules in `StreamAccumulator` and `handle_core_event()`.\n\n22 variants (20 from design §4.1 + 2 coco-rs extensions).",
  "oneOf": [
    {
      "description": "Permission approval overlay needed.\n\nWhen `choices` is `Some`, the TUI should render a multi-choice list (e.g. ExitPlanMode's keep/clear/cancel) instead of the default yes/no buttons. Picked `value` is echoed back via `UserCommand::ApprovalResponse.updated_input` as a `{ ..original_input, user_choice: \"<value>\" }` JSON object so the tool's `execute()` can branch on the choice. TS parity: `ExitPlanModePermissionRequest.tsx:691-704` option grid.\n\n`original_input` carries the raw tool input so the TUI can splice the picked `user_choice` into it verbatim and derive path-scoped read permission updates for classic \"always allow\" approvals.",
      "type": "object",
      "required": [
        "description",
        "input_preview",
        "request_id",
        "tool_name",
        "type"
      ],
      "properties": {
        "choices": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/PermissionAskChoice"
          }
        },
        "description": {
          "type": "string"
        },
        "input_preview": {
          "type": "string"
        },
        "original_input": true,
        "permission_suggestions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PermissionUpdate"
          }
        },
        "request_id": {
          "type": "string"
        },
        "show_always_allow": {
          "description": "Whether the UI may offer an \"always allow\" action. False when managed policy restricts local/session rule changes.",
          "default": false,
          "type": "boolean"
        },
        "tool_name": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "approval_required"
          ]
        }
      }
    },
    {
      "description": "AskUserQuestion overlay needed. `input` carries the full tool input dict (the `questions[]` array, etc.) verbatim so the TUI renders the rich multi-question UI (multiSelect, preview, notes) matching TS `AskUserQuestionPermissionRequest.tsx`. The TUI echoes the answer payload back via `UserCommand::ApprovalResponse` with `updated_input = Some({...input, answers, annotations})`.",
      "type": "object",
      "required": [
        "input",
        "request_id",
        "type"
      ],
      "properties": {
        "input": true,
        "request_id": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "question_asked"
          ]
        }
      }
    },
    {
      "description": "MCP elicitation overlay needed.",
      "type": "object",
      "required": [
        "request_id",
        "schema",
        "server",
        "type"
      ],
      "properties": {
        "request_id": {
          "type": "string"
        },
        "schema": true,
        "server": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "elicitation_requested"
          ]
        }
      }
    },
    {
      "description": "Sandbox approval overlay needed.",
      "type": "object",
      "required": [
        "operation",
        "request_id",
        "type"
      ],
      "properties": {
        "operation": {
          "type": "string"
        },
        "request_id": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "sandbox_approval_required"
          ]
        }
      }
    },
    {
      "description": "Plugin picker data loaded.",
      "type": "object",
      "required": [
        "plugins",
        "type"
      ],
      "properties": {
        "plugins": {
          "type": "array",
          "items": true
        },
        "type": {
          "type": "string",
          "enum": [
            "plugin_data_ready"
          ]
        }
      }
    },
    {
      "description": "Output style picker data loaded.",
      "type": "object",
      "required": [
        "styles",
        "type"
      ],
      "properties": {
        "styles": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "type": {
          "type": "string",
          "enum": [
            "output_styles_ready"
          ]
        }
      }
    },
    {
      "description": "Slash-command catalogue changed — sent after the seed at session start AND whenever `/reload-plugins` swaps the active `CommandRegistry`. The TUI overwrites `state.session.available_commands` with this list so the `/` autocomplete popup and command palette stay in sync. TS parity: the TS popup re-queries `getCommands()` inline; coco-rs pushes the snapshot because the registry lives in the CLI process.",
      "type": "object",
      "required": [
        "commands",
        "type"
      ],
      "properties": {
        "commands": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SlashCommandInfo"
          }
        },
        "type": {
          "type": "string",
          "enum": [
            "available_commands_refreshed"
          ]
        }
      }
    },
    {
      "description": "Rewind selector checkpoints loaded.",
      "type": "object",
      "required": [
        "checkpoints",
        "type"
      ],
      "properties": {
        "checkpoints": {
          "type": "array",
          "items": true
        },
        "type": {
          "type": "string",
          "enum": [
            "rewind_checkpoints_ready"
          ]
        }
      }
    },
    {
      "description": "Rewind diff preview loaded.",
      "type": "object",
      "required": [
        "deletions",
        "files_changed",
        "insertions",
        "message_id",
        "type"
      ],
      "properties": {
        "deletions": {
          "type": "integer",
          "format": "int64"
        },
        "file_paths": {
          "description": "Path strings of changed files in display order. Empty when `files_changed == 0`. TS source: `DiffStats.filesChanged: string[]` (`utils/fileHistory.ts:55-61`). Used by the rewind picker to render basenames per row and the selected-file label on the confirm screen. `#[serde(default)]` keeps the wire backwards-compatible with older producers that didn't populate it.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "files_changed": {
          "type": "integer",
          "format": "int32"
        },
        "insertions": {
          "type": "integer",
          "format": "int64"
        },
        "message_id": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "diff_stats_ready"
          ]
        }
      }
    },
    {
      "description": "Compaction circuit breaker opened.",
      "type": "object",
      "required": [
        "failures",
        "type"
      ],
      "properties": {
        "failures": {
          "type": "integer",
          "format": "int32"
        },
        "type": {
          "type": "string",
          "enum": [
            "compaction_circuit_breaker_open"
          ]
        }
      }
    },
    {
      "description": "Micro-compaction applied notification.",
      "type": "object",
      "required": [
        "removed",
        "type"
      ],
      "properties": {
        "removed": {
          "type": "integer",
          "format": "int32"
        },
        "type": {
          "type": "string",
          "enum": [
            "micro_compaction_applied"
          ]
        }
      }
    },
    {
      "description": "Session memory compaction applied notification.",
      "type": "object",
      "required": [
        "summary_tokens",
        "type"
      ],
      "properties": {
        "summary_tokens": {
          "type": "integer",
          "format": "int32"
        },
        "type": {
          "type": "string",
          "enum": [
            "session_memory_compact_applied"
          ]
        }
      }
    },
    {
      "description": "Speculative execution rolled back.",
      "type": "object",
      "required": [
        "reason",
        "type"
      ],
      "properties": {
        "reason": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "speculative_rolled_back"
          ]
        }
      }
    },
    {
      "description": "Memory extraction started.",
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "session_memory_extraction_started"
          ]
        }
      }
    },
    {
      "description": "Memory extraction completed.",
      "type": "object",
      "required": [
        "extracted",
        "type"
      ],
      "properties": {
        "extracted": {
          "type": "integer",
          "format": "int32"
        },
        "type": {
          "type": "string",
          "enum": [
            "session_memory_extraction_completed"
          ]
        }
      }
    },
    {
      "description": "Memory extraction failed.",
      "type": "object",
      "required": [
        "error",
        "type"
      ],
      "properties": {
        "error": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "session_memory_extraction_failed"
          ]
        }
      }
    },
    {
      "description": "Cron job disabled by circuit breaker.",
      "type": "object",
      "required": [
        "job_id",
        "reason",
        "type"
      ],
      "properties": {
        "job_id": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "cron_job_disabled"
          ]
        }
      }
    },
    {
      "description": "Missed cron job fires.",
      "type": "object",
      "required": [
        "count",
        "type"
      ],
      "properties": {
        "count": {
          "type": "integer",
          "format": "int32"
        },
        "type": {
          "type": "string",
          "enum": [
            "cron_jobs_missed"
          ]
        }
      }
    },
    {
      "description": "Streaming tool input delta (typing effect).\n\n# Status: reserved scaffolding, not yet wired\n\nThe TUI has a handler (`server_notification_handler::handle_tui_only`) that appends the delta to `ToolExecution.streaming_input` for a typing-effect display, but **no producer currently emits this variant** in coco-rs.\n\nThe inference layer's `StreamEvent::ToolCallDelta` (a different type, internal to `coco-inference`) is fully accumulated into the complete tool input before the engine emits `AgentStreamEvent::ToolUseQueued` with the finalized input. Consumers see the complete input at once.\n\nFuture work to wire this up would require the inference layer to forward the partial JSON fragments alongside the accumulation, and the engine to emit them here as `CoreEvent::Tui(ToolCallDelta { ... })`.\n\n# Why keep it in TuiOnlyEvent (not AgentStreamEvent)\n\nPer `event-system-design.md` §3.3: partial JSON deltas serve a purely UI display purpose (typing effect) and the SDK does not need them — `ToolUseQueued` already contains the complete input. Promoting to `AgentStreamEvent` would burden SDK consumers with partial JSON they must re-assemble, with no behavioral benefit.",
      "type": "object",
      "required": [
        "call_id",
        "delta",
        "type"
      ],
      "properties": {
        "call_id": {
          "type": "string"
        },
        "delta": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "tool_call_delta"
          ]
        }
      }
    },
    {
      "description": "Tool progress update (progress bar).",
      "type": "object",
      "required": [
        "data",
        "tool_use_id",
        "type"
      ],
      "properties": {
        "data": true,
        "tool_use_id": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "tool_progress"
          ]
        }
      }
    },
    {
      "description": "Tool execution aborted notification.",
      "type": "object",
      "required": [
        "reason",
        "tool_use_id",
        "type"
      ],
      "properties": {
        "reason": {
          "type": "string"
        },
        "tool_use_id": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "tool_execution_aborted"
          ]
        }
      }
    },
    {
      "description": "Rewind completed — TUI truncates messages and restores input state. coco-rs extension: UI-only because it carries TUI-specific identifiers for message truncation and input repopulation. Out-of-band from the design's `rewind/completed` ServerNotification which carries protocol metadata only.",
      "type": "object",
      "required": [
        "files_changed",
        "target_message_id",
        "type"
      ],
      "properties": {
        "files_changed": {
          "description": "Number of files restored (0 if conversation-only).",
          "type": "integer",
          "format": "int32"
        },
        "target_message_id": {
          "description": "UUID of the target user message. Empty = code-only rewind.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "rewind_completed"
          ]
        }
      }
    },
    {
      "description": "Local slash-command produced a `CommandResult::Text` (or `Compact { display_text }`). Surfaced as a system-role chat message so it reads inline with the transcript. `text` is the pre-rendered body — never translated, since it carries the handler's actual output (often command-specific status / git output / prompt preview).",
      "type": "object",
      "required": [
        "name",
        "text",
        "type"
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "text": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "slash_command_result"
          ]
        }
      }
    },
    {
      "description": "Dispatcher-side breadcrumb for slash commands the runtime couldn't fully execute (missing handler, handler error, empty Prompt body, dialog wiring pending). The TUI translates `kind` via the i18n catalog before rendering.",
      "type": "object",
      "required": [
        "kind",
        "name",
        "type"
      ],
      "properties": {
        "kind": {
          "$ref": "#/definitions/SlashCommandStatusKind"
        },
        "name": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "slash_command_status"
          ]
        }
      }
    },
    {
      "description": "Tell the TUI to open the rewind picker overlay. Emitted when the slash dispatcher resolves `/rewind` from the command palette (typed `/rewind` is intercepted earlier in `update/edit.rs` without round-tripping through `CoreEvent`). The TUI consumes the current `session.messages` to build the picker.",
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "open_rewind_picker"
          ]
        }
      }
    },
    {
      "description": "Tell the TUI to open the `/memory` file-picker overlay. The slash dispatcher pre-builds entries via `coco_commands::handlers::memory_dialog::MemoryDialogHandler::entries` and carries them through here so the TUI doesn't recompute paths. On selection the TUI sends an `OpenMemoryFile` command to the CLI bridge; the bridge creates the file (`mode wx` semantics), opens `$VISUAL || $EDITOR`, and reports the result back as a transcript visible event. On cancel the TUI emits \"Cancelled memory editing\". TS parity: `commands/memory/memory.tsx`.",
      "type": "object",
      "required": [
        "entries",
        "type"
      ],
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/MemoryDialogEntry"
          }
        },
        "type": {
          "type": "string",
          "enum": [
            "open_memory_dialog"
          ]
        }
      }
    },
    {
      "description": "`/memory` editor launch completed successfully.",
      "type": "object",
      "required": [
        "path",
        "type"
      ],
      "properties": {
        "path": {
          "description": "Path of the file passed to the editor.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "memory_file_opened"
          ]
        }
      }
    },
    {
      "description": "`/memory` editor launch failed before a process was started.",
      "type": "object",
      "required": [
        "error",
        "path",
        "type"
      ],
      "properties": {
        "error": {
          "description": "User-visible failure summary.",
          "type": "string"
        },
        "path": {
          "description": "Path of the file the user selected.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "memory_file_open_failed"
          ]
        }
      }
    },
    {
      "description": "Plan editor launch completed successfully.",
      "type": "object",
      "required": [
        "path",
        "type"
      ],
      "properties": {
        "path": {
          "description": "Path of the session plan file passed to the editor.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "plan_file_opened"
          ]
        }
      }
    },
    {
      "description": "Plan editor launch failed before a process was started.",
      "type": "object",
      "required": [
        "error",
        "path",
        "type"
      ],
      "properties": {
        "error": {
          "description": "User-visible failure summary.",
          "type": "string"
        },
        "path": {
          "description": "Path of the session plan file.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "plan_file_open_failed"
          ]
        }
      }
    },
    {
      "description": "Request the foreground TUI to leave raw mode / alt-screen before the CLI runner starts an interactive editor process.",
      "type": "object",
      "required": [
        "request_id",
        "type"
      ],
      "properties": {
        "request_id": {
          "description": "Opaque request id echoed back by the TUI once terminal modes are ready for the external editor.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "external_editor_prepare"
          ]
        }
      }
    },
    {
      "description": "External prompt editor completed.",
      "type": "object",
      "required": [
        "content",
        "modified",
        "type"
      ],
      "properties": {
        "content": {
          "description": "Edited prompt content read back from the temp file.",
          "type": "string"
        },
        "modified": {
          "description": "Whether the edited content differs from the initial content.",
          "type": "boolean"
        },
        "type": {
          "type": "string",
          "enum": [
            "prompt_editor_completed"
          ]
        }
      }
    },
    {
      "description": "External prompt editor failed before content could be read back.",
      "type": "object",
      "required": [
        "error",
        "type"
      ],
      "properties": {
        "error": {
          "description": "User-visible failure summary.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "prompt_editor_failed"
          ]
        }
      }
    },
    {
      "description": "Result of a prompt-mode `!`-prefixed bash submission. The TUI folds this into a `MessageContent::BashOutput` chat message keyed by the same `user_message_id` as the matching `BashInput`. Emitted by the CLI bridge after running the command via `coco_shell::ShellExecutor`. TS parity: `LocalShellTask` completion.",
      "type": "object",
      "required": [
        "exit_code",
        "output",
        "type",
        "user_message_id"
      ],
      "properties": {
        "exit_code": {
          "description": "Process exit code; non-zero shades the output red.",
          "type": "integer",
          "format": "int32"
        },
        "output": {
          "description": "Merged stdout + stderr, already truncated to a reasonable display size by the bridge.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "bash_command_completed"
          ]
        },
        "user_message_id": {
          "description": "Shared id of the bash input/output pair so rewind groups them.",
          "type": "string"
        }
      }
    },
    {
      "description": "Tell the TUI to open the provider-grouped model picker. Emitted when the slash dispatcher resolves `/model` with no args (typed `/model` from input bar). The TUI consumes the current `state.session.model` to mark the \"current\" entry.",
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "open_model_picker"
          ]
        }
      }
    }
  ],
  "definitions": {
    "MemoryDialogEntry": {
      "description": "One row in the `/memory` file-picker overlay. Built by the slash dispatcher and shipped to the TUI via [`TuiOnlyEvent::OpenMemoryDialog`].",
      "type": "object",
      "required": [
        "label",
        "path",
        "scope"
      ],
      "properties": {
        "label": {
          "description": "Display label shown in the picker row.",
          "type": "string"
        },
        "path": {
          "description": "Filesystem path of the memory file (also the editor target).",
          "type": "string"
        },
        "row_kind": {
          "description": "Row semantics for rendering and selection behavior.",
          "default": {
            "exists": false,
            "kind": "file",
            "read_only": false
          },
          "allOf": [
            {
              "$ref": "#/definitions/MemoryDialogRowKind"
            }
          ]
        },
        "scope": {
          "description": "Scope tag (drives ordering and color hint).",
          "allOf": [
            {
              "$ref": "#/definitions/MemoryDialogScope"
            }
          ]
        }
      }
    },
    "MemoryDialogRowKind": {
      "description": "Semantic row kind for the `/memory` picker.",
      "oneOf": [
        {
          "description": "Editable memory file.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "exists": {
              "description": "Whether the file already exists at event construction time.",
              "default": false,
              "type": "boolean"
            },
            "kind": {
              "type": "string",
              "enum": [
                "file"
              ]
            },
            "read_only": {
              "description": "Whether the row should be treated as read-only by future UIs.",
              "default": false,
              "type": "boolean"
            }
          }
        },
        {
          "description": "Folder grouping row for future memory directory surfaces.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "enabled": {
              "description": "Whether the folder is currently enabled.",
              "default": false,
              "type": "boolean"
            },
            "kind": {
              "type": "string",
              "enum": [
                "folder"
              ]
            }
          }
        },
        {
          "description": "Toggle row for future auto-memory settings.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "enabled": {
              "description": "Current toggle state.",
              "default": false,
              "type": "boolean"
            },
            "kind": {
              "type": "string",
              "enum": [
                "toggle"
              ]
            }
          }
        }
      ]
    },
    "MemoryDialogScope": {
      "description": "Scope tag for a memory file picker entry. Mirrors `coco_commands::MemoryScope` — kept in `coco-types` so the TUI can consume the event without depending on `coco-commands`.",
      "oneOf": [
        {
          "description": "Enterprise / managed (typically read-only for the user).",
          "type": "string",
          "enum": [
            "managed"
          ]
        },
        {
          "description": "User-global (`~/.coco/CLAUDE.md` or `~/CLAUDE.md`).",
          "type": "string",
          "enum": [
            "user"
          ]
        },
        {
          "description": "Project (`./CLAUDE.md`).",
          "type": "string",
          "enum": [
            "project"
          ]
        },
        {
          "description": "Project-local (`./CLAUDE.local.md`, gitignored).",
          "type": "string",
          "enum": [
            "project_local"
          ]
        },
        {
          "description": "Subdirectory CLAUDE.md (auto-loaded under cwd).",
          "type": "string",
          "enum": [
            "subdir"
          ]
        }
      ]
    },
    "PermissionAskChoice": {
      "description": "One option in a multi-choice permission dialog.\n\nUsed by `ToolCheckResult::Ask.choices` and surfaced on the wire via `PermissionDecision::Ask.choices`. The TUI renders one row per choice; the picked `value` is echoed back so the tool's `execute()` can branch on the user's selection.",
      "type": "object",
      "required": [
        "label",
        "value"
      ],
      "properties": {
        "description": {
          "description": "Optional one-line explanation rendered under the label.",
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "description": "Short row label shown to the user.",
          "type": "string"
        },
        "value": {
          "description": "Stable identifier echoed back in the approval response. Use kebab-case (`\"yes-keep-context\"`, `\"yes-clear-context\"`, `\"no\"`).",
          "type": "string"
        }
      }
    },
    "PermissionBehavior": {
      "description": "Permission behavior for a rule.",
      "type": "string",
      "enum": [
        "allow",
        "deny",
        "ask"
      ]
    },
    "PermissionMode": {
      "description": "Permission mode (top-level because it's used by both message and permission modules).\n\nWire format is camelCase to match TS `PermissionModeSchema` at `coreSchemas.ts:337-347`: `z.enum(['default', 'acceptEdits', 'bypassPermissions', 'plan', 'dontAsk'])`. The serde aliases on the drifting variants accept legacy snake_case input so old session transcripts deserialize cleanly.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "default",
            "plan",
            "bypassPermissions",
            "dontAsk",
            "acceptEdits"
          ]
        },
        {
          "description": "Feature-gated auto mode.",
          "type": "string",
          "enum": [
            "auto"
          ]
        },
        {
          "description": "Internal: escalate to parent agent.",
          "type": "string",
          "enum": [
            "bubble"
          ]
        }
      ]
    },
    "PermissionRule": {
      "description": "A single permission rule.",
      "type": "object",
      "required": [
        "behavior",
        "source",
        "value"
      ],
      "properties": {
        "behavior": {
          "$ref": "#/definitions/PermissionBehavior"
        },
        "source": {
          "$ref": "#/definitions/PermissionRuleSource"
        },
        "value": {
          "$ref": "#/definitions/PermissionRuleValue"
        }
      }
    },
    "PermissionRuleSource": {
      "description": "Source of a permission rule (ordered by priority: Session is most specific).",
      "type": "string",
      "enum": [
        "user_settings",
        "project_settings",
        "local_settings",
        "flag_settings",
        "policy_settings",
        "cli_arg",
        "command",
        "session"
      ]
    },
    "PermissionRuleValue": {
      "description": "Permission rule value — tool_pattern is a glob/wildcard expression. Examples: \"Read\", \"Bash(git *)\", \"mcp__slack__*\", \"*\"",
      "type": "object",
      "required": [
        "tool_pattern"
      ],
      "properties": {
        "rule_content": {
          "description": "Command pattern within tool (e.g. \"git *\").",
          "type": [
            "string",
            "null"
          ]
        },
        "tool_pattern": {
          "type": "string"
        }
      }
    },
    "PermissionUpdate": {
      "description": "A permission update action.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "destination",
            "rules",
            "type"
          ],
          "properties": {
            "destination": {
              "$ref": "#/definitions/PermissionUpdateDestination"
            },
            "rules": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PermissionRule"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "add_rules"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "destination",
            "rules",
            "type"
          ],
          "properties": {
            "destination": {
              "$ref": "#/definitions/PermissionUpdateDestination"
            },
            "rules": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PermissionRule"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "replace_rules"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "destination",
            "rules",
            "type"
          ],
          "properties": {
            "destination": {
              "$ref": "#/definitions/PermissionUpdateDestination"
            },
            "rules": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PermissionRule"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "remove_rules"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "mode",
            "type"
          ],
          "properties": {
            "mode": {
              "$ref": "#/definitions/PermissionMode"
            },
            "type": {
              "type": "string",
              "enum": [
                "set_mode"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "destination",
            "directories",
            "type"
          ],
          "properties": {
            "destination": {
              "$ref": "#/definitions/PermissionUpdateDestination"
            },
            "directories": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "add_directories"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "destination",
            "directories",
            "type"
          ],
          "properties": {
            "destination": {
              "$ref": "#/definitions/PermissionUpdateDestination"
            },
            "directories": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "remove_directories"
              ]
            }
          }
        }
      ]
    },
    "PermissionUpdateDestination": {
      "description": "Destination for persisting permission updates.\n\nPersistable destinations (`User`/`Project`/`LocalSettings`) write to disk; in-memory destinations (`Session`/`CliArg`/`Command`) live only for the running session. TS parity: same split as `persistPermissionUpdates` in `PermissionUpdate.ts`.\n\n`Command` is reserved for rules contributed by an invoked command or skill's frontmatter (`allowed-tools:`). TS parity: `alwaysAllowRules.command` populated by `SkillTool` / `createGetAppStateWithAllowedTools`.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "user_settings",
            "project_settings",
            "local_settings",
            "session",
            "cli_arg"
          ]
        },
        {
          "description": "Rules contributed by a command/skill frontmatter `allowed-tools`. In-memory only; cleared on session end.",
          "type": "string",
          "enum": [
            "command"
          ]
        }
      ]
    },
    "SlashCommandInfo": {
      "description": "UI-facing projection of a slash command. The TUI receives a `Vec` of these at startup (and again after `/reload-plugins`) so the autocomplete popup and command palette can render and rank without reaching into [`CommandBase`] every time.\n\nLives in `coco-types` (rather than `coco-tui`) so it can travel on a [`crate::TuiOnlyEvent`] variant — events are the only path between the agent driver and the TUI, and event payload types must be foundation-layer.",
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "aliases": {
          "description": "Alternate names that also match this command. Searched by the ranker so `/cls` finds `/clear` when `cls` is an alias.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "argument_hint": {
          "description": "Hint string rendered next to the description when the command takes arguments (e.g. `\"<file>\"` for `/add-dir`). Mirrors [`CommandBase::argument_hint`].",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "Short description shown dimmed in the popup. `None` when the source command registered without one.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Canonical command name without the leading `/`.",
          "type": "string"
        }
      }
    },
    "SlashCommandStatusKind": {
      "description": "Categorization of a `SlashCommandStatus` payload. Each variant maps to a `slash.status.*` key in the TUI locale catalog.\n\nWire format intentionally tagged so SDK clients can render their own localized strings instead of consuming the TUI's English fallback.",
      "oneOf": [
        {
          "description": "Registered command has no `handler` — typically a plugin contribution stub that wasn't bridged.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "no_handler"
              ]
            }
          }
        },
        {
          "description": "Handler returned `Err`. `error` is the formatted error message.",
          "type": "object",
          "required": [
            "error",
            "kind"
          ],
          "properties": {
            "error": {
              "type": "string"
            },
            "kind": {
              "type": "string",
              "enum": [
                "failed"
              ]
            }
          }
        },
        {
          "description": "Handler returned `CommandResult::Prompt` with no text parts.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "empty_prompt"
              ]
            }
          }
        },
        {
          "description": "Handler returned `CommandResult::OpenDialog`, but coco-rs has not yet bound this dialog kind to a TUI overlay. `dialog_kind` is a human-readable label like \"memory file selector\".",
          "type": "object",
          "required": [
            "dialog_kind",
            "kind"
          ],
          "properties": {
            "dialog_kind": {
              "type": "string"
            },
            "kind": {
              "type": "string",
              "enum": [
                "dialog_pending"
              ]
            }
          }
        },
        {
          "description": "`/permissions allow` invoked with no tool name. Dispatcher-side usage hint — the TUI translates via `slash.permissions.usage_allow`.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "permissions_usage_allow"
              ]
            }
          }
        },
        {
          "description": "`/permissions deny` invoked with no tool name. Dispatcher-side usage hint — the TUI translates via `slash.permissions.usage_deny`.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "permissions_usage_deny"
              ]
            }
          }
        }
      ]
    }
  }
}