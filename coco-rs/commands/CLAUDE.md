# coco-commands

Slash command system: registry + ~96 command implementations across v1/v2/v3.

## TS Source
- `src/commands.ts` (command registry)
- `src/commands/` (~88 directories -- help, config, clear, diff, doctor, bug, compact, session, login, mcp, plan, plugin, pr_comments, tasks, agents, skills, etc.)

## Key Types
Command trait, CommandRegistry, CommandContext
