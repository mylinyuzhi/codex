# coco-cli

CLI entry point (clap), transports (SSE, WS, NDJSON), server/daemon mode.

## TS Source
- `src/entrypoints/` (CLI entry points)
- `src/main.tsx` (803K -- main entry, CLI bootstrapping)
- `src/cli/` (handlers/, transports/)
- `src/server/` (server/daemon mode)
- `src/utils/releaseNotes.ts`

## Key Types
Cli (clap), Commands, Transport
