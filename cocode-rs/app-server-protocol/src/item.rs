//! Thread item types — re-exported from `cocode-protocol`.
//!
//! The canonical definitions live in `cocode_protocol::server_notification::item`.

pub use cocode_protocol::server_notification::item::*;
