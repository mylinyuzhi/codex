# coco-permissions

Permission evaluation pipeline, auto-mode/yolo classifier (2-stage XML via LLM), denial tracking.

## TS Source
- `src/utils/permissions/` (rule evaluation, auto-mode classifier, denial tracking)

## Key Types
PermissionEvaluator, PermissionDecision, DenialTracker, AutoModeClassifier
